# BlazeLink 鏈客行銷 設計系統

## 公司與產品背景

BlazeLink 鏈客行銷（鏈客策略行銷股份有限公司）是一家台灣的 MarTech 行銷顧問公司，主打「自動化獲客引擎」——為 B2B 企業、知識型服務與跨國品牌，整合 SEO、網站轉換漏斗、行銷自動化與數據分析，把專業知識轉化為可預測的名單與營收。核心服務包含精準行銷策略、MarTech 自動化銷售漏斗、網站架設／客製開發、跨國 SEO 內容行銷、市場洞察／競品分析、AI 導入與 API 串接。

團隊定位是「高階 MarTech 顧問 + 技術型創業夥伴」，強調不做傳統網頁美編，只對「最終轉換與名單」負責；擅長跨國（尤其歐美 B2B / YMYL 高門檻產業）SEO 落地與 EEAT 信任架構建置。

目前唯一的產品/門面是官方行銷網站 **blazelink.co**（WordPress + WooCommerce 佈景主題架設，非自建產品，未見獨立設計系統或元件庫），提供中／英／日三語版本，並附設「鏈客商學院」部落格。

**來源**：
- 官網：https://blazelink.co （首頁、關於我們、跨國SEO行銷等頁面內容）
- 使用者提供的 logo 檔案：`uploads/鏈客Logo-Horizontal.png`
- 未提供 Figma 檔案、程式碼庫或既有設計規範 —— 本設計系統的視覺基礎（色彩、字體配對、圓角、陰影尺度）是從既有 logo 與網站文案語氣「向上萃取」重新定義的一套全新品牌系統，供未來的設計/原型/簡報使用。

## 內容基本原則 CONTENT FUNDAMENTALS

**語言與人稱**：主要語言為繁體中文（台灣），對讀者一律使用敬語「您」。文案常見「我們／您」的夥伴式對句，強調共同成長而非單向推銷。例如：
> 「我們就像是你的事業夥伴！」
> 「我們的成功建立在您的成功之上，這就是我們為之努力的原因。」

**語氣**：顧問式、自信、直球對決，不打高空。大量使用「問題 → 對比 → 解方」的三段式結構，先拋出痛點反問句，再用「傳統做法 vs. 鏈客做法」的對照凸顯差異：
> 「為什麼您的網站留不住客戶？」
> 「傳統網頁設計只在乎『好看』，鏈客增長引擎專注於『變現』。」

**專有名詞混用**：中英夾雜是常態且刻意保留，MarTech、SEO、ROI、EEAT、CRM、Hreflang、API/Webhook 等術語直接以英文呈現，不強行翻譯，塑造專業／國際感。

**強調與結構符號**：
- 用 ❌ / ✅ 做「傳統做法 vs. 我們的做法」條列對比，是最鮮明的文案 signature，之後在元件系統中延伸為 Alert／Comparison 的正負向樣式。
- 用 `/ 標題` 這種前綴斜線標記段落眉標（如「/ 公司理念」「/ 合作方案」「/ 經驗分享」）。
- 用 `01 /`「02 /」做服務清單編號。
- 團隊成員的專長標籤以全形｜分隔（「行銷科技與自動化｜精準行銷策略｜全端開發」）。
- 粗體大量用於句中關鍵詞強調，而非整句加粗。

**行動呼籲（CTA）**：一律是具體動詞開頭的短句，如「查看完整方案」「立即聊聊您的海外計畫」「打造你的國際獲客系統」——賣的是「系統」與「引擎」而非單次服務。

**沒有 emoji 濫用**：除了 ❌ ✅ 這組語意符號與少量箭頭（⬇️）外，正文不使用裝飾性表情符號，維持顧問公司的專業調性。

**vibe**：精準、理性、技術驅動，但用「冒險夥伴」「共創成功」等措辭保留溫度——是「懂技術的顧問」而非「冷冰冰的代理商」。

## 視覺基礎 VISUAL FOUNDATIONS

> 官網本身是通用 WordPress 佈景主題，沒有獨立設計系統可萃取。以下視覺基礎是**以現有 logo 漸層與品牌調性為種子，全新定義**的一套系統，非照抄官網現況。

**色彩**：品牌識別核心是 logo 上從紫到藍到青的三段漸層（取樣值：`#7C5CFC → #5E9BFB → #4FC3F7`），象徵「連結」與「數據流動」。此漸層保留為**品牌簽名色**，用於首屏標題重點字、圖示背景、分隔線與行銷素材的視覺焦點，不濫用在大面積背景。UI 元件（按鈕、連結、表單焦點）改用漸層中最深的純色 `--color-brand-violet #7C5CFC` 作為主要動作色，確保文字對比與可及性。中性色採用「冷調石板灰」（藍紫調的灰階），從 `--slate-50` 到 `--slate-950`，避免暖灰破壞冷色調品牌感。語意色（成功/警示/危險）刻意選擇獨立色相，不與品牌漸層混色，呼應官網已經在用的 ✅ 綠 / ❌ 紅對比語言。

**字體**：標題／英文展示字使用 **Poppins**（幾何無襯線、圓潤字懷，與 logo「BLAZELINK」字標的幾何感一致），內文與中文一律使用 **Noto Sans TC**；因 Poppins 不含中文字符，中文標題會自動 fallback 到 Noto Sans TC，形成「英文用 Poppins、中文用 Noto Sans TC」的雙字體無縫切換，不需要手動判斷語言。字重集中在 400/500/600/700，避免過細字重在中文渲染下失真。

**間距**：4px 為基礎顆粒，衍生 4/8/12/16/20/24/32/40/48/64/80/96/128 的等比尺度；頁面容器最大寬度 1200px，區塊間距預設 24px（gutter）。

**背景**：以純色卡片與大留白為主，不使用重複紋理或手繪插畫（官網素材以真人團隊照片與圖表 icon 為主，沒有插畫風格）。品牌漸層可作為局部裝飾（分隔線、圖示底色、hero 區塊的強調文字），但避免整版大面積漸層背景，維持 B2B 顧問公司的穩重感。

**圓角**：偏「銳利」尺度 —— 一般控制項 4–8px（`--radius-sm/md/lg`），大型卡片最高 12px（`--radius-xl`），僅標籤（Tag/Badge）與膠囊式按鈕使用全圓角（`--radius-pill`）。不使用大圓角、糖果感的卡片。

**陰影**：冷色調、低擴散的柔和陰影（以 `--slate-950` 為底色取極低透明度），三階：sm（1px 卡片邊界感）、md（懸浮卡片）、lg（彈出層/對話框）。不使用內陰影或彩色陰影。

**動效**：一致使用 `cubic-bezier(.4,0,.2,1)` 標準緩動；互動反饋以淡入淡出＋輕微位移為主（150–220ms），不使用彈跳（bounce）或誇張的縮放動畫，符合顧問品牌的穩重調性。

**Hover / Press 狀態**：Hover 採「加深純色」（violet → `--color-primary-hover`），而非變淡或加陰影；Press／Active 再進一步加深一階（`--color-primary-active`），並可搭配 1px 輕微位移，不做明顯縮放。次要按鈕與連結的 hover 以底線／背景色微變化呈現。

**邊框**：預設 1px 實線，顏色取自中性色階（`--border-default`），不使用彩色邊框；卡片以「淺灰邊框 + 極淡陰影」二選一或並用，不使用左側色條裝飾（避免落入常見 AI 生成介面的陳腔濫調）。

**透明度與模糊**：目前無明確使用場景，僅在 Overlay/Modal 遮罩使用 `rgba(11,14,26,.4)` 純色透明，不使用毛玻璃（backdrop-blur），維持清晰銳利的介面感。

**圖像色調**：官網照片以自然光的人像與辦公場景為主，偏冷白平衡，無濾鏡濾色、無顆粒感（grain）處理，維持乾淨、可信賴的商務調性。

## 圖標系統 ICONOGRAPHY

官網 `/global-seo/` 頁面的圖示檔名（`headphone-line.png`、`account-circle-line.png`、`stack-line.png`）直接對應 **RemixIcon**（remixicon.com）的線性圖示命名慣例，可判斷官網原本即採用 RemixIcon 的 line 風格圖示集。本設計系統延續同一套圖示系統：

- 一律使用 RemixIcon 的 **line（線性）** 風格，不混用 fill（實心）風格，維持線條纖細、單一像素粗細的一致感。
- 透過 CDN 引入：`<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css">`，以 `<i class="ri-headphone-line"></i>` 使用。
- 圖示顏色預設跟隨文字顏色（`currentColor`），強調時可套用品牌紫 `--color-brand-violet`。
- 不使用 emoji 作為功能性圖示（僅官網文案中的 ❌ / ✅ 屬於語意符號，不算圖標系統的一部分，已在元件中對應為 Alert 的正負向樣式）。
- 未見官網使用自訂 SVG 插畫圖示或 PNG 圖示包，故不額外引入其他圖示集。

## 元件庫 Intentional additions

官網沒有可萃取的既有元件庫或程式碼庫，因此本系統從零建立一組精簡的標準元件（非既有元件的 1:1 複製），聚焦在行銷網站／顧問品牌最常用的介面：

- **Button** — 主要 CTA 用（漸層/純色可選）
- **Badge** — 服務標籤、狀態標示
- **Tag** — 團隊專長標籤（對應官網「｜」分隔的技能標籤）
- **Card** — 團隊成員卡、部落格文章卡、服務卡
- **Input** — 聯繫表單欄位
- **Alert** — 對應官網 ❌ / ✅ 對比條列與提醒文字（如「※ 我們不採用強迫推銷」）

## 索引 Index

```
styles.css              全域樣式入口（只放 @import）
tokens/
  colors.css             品牌漸層／中性色／語意色
  typography.css          Poppins + Noto Sans TC，含 Google Fonts 引入
  spacing.css             間距與容器寬度
  effects.css             圓角／陰影／動效
assets/
  logo-horizontal.png     官方橫式 logo（使用者提供）
  logo-icon.png           從橫式 logo 裁切出的圖標標記
guidelines/               Design System 分頁的 specimen 卡片（顏色/字體/間距/品牌/圖標）
components/core/          Button, Badge, Tag, Card, Input, Alert（.jsx + .d.ts + .prompt.md + 卡片 html）
SKILL.md                 可攜式技能檔（供 Claude Code 等環境使用）
```
